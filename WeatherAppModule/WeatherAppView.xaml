<UserControl x:Class="LEARN_MVVM.WeatherAppModule.WeatherAppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LEARN_MVVM.WeatherAppModule"
             xmlns:viewModel="clr-namespace:LEARN_MVVM"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#272537"
             
             d:DataContext="{d:DesignInstance Type=local:WeatherAppViewModel}">
    <UserControl.DataContext>
        <local:WeatherAppViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ui:TextBox Foreground="White" 
                    FontSize="14"
                    PlaceholderText="Type city name" 
                    TextWrapping="Wrap" 
                    Grid.Column="1" 
                    Margin="-50,40,10,20" 
                    Text="{Binding City}"/>
        <TextBlock Foreground="White" 
                   Text="{Binding Temp}"
                   TextWrapping="Wrap"
                   Grid.Column="1" Grid.Row="1"
                   FontSize="14"
                   Margin="-50,40,20,40"/>
        <ui:Button Content="Search" 
                   Height="40" Width="100" 
                   Grid.Column="2" Grid.Row="0" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Left"
                   Margin="20"
                   Command="{Binding SearchTempCommand, Mode=OneWay}"/>
        <ui:Button Content="Clear" 
                   Height="40" Width="100" 
                   Grid.Column="2" Grid.Row="1" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Left" 
                   FontSize="14"
                   Margin="20"
                   Command="{Binding ClearBoxCommand}"/>
    </Grid>
</UserControl>